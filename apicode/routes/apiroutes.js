/*This is same as index.js -- in case routs needs to be kept separted*/


/*
var express = require('express');
var router = express.Router();
//var models = require('./apicode/models/cmodel.js');


router.get('/health', function(req, res, next) {
  res.send("Yes, this endpoint is working");
  console.log(res.value);
});

router.post('/main',function(req,res,next){
  console.log(req.body.queryResult.queryText);
  res.json({fulfillmentText: "This is the default message from hook"});
});

module.exports = router;*/